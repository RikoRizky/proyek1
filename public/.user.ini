; Batas unggahan untuk lingkungan yang membaca .user.ini (mis. PHP-FPM/CGI).
; Untuk `php artisan serve`, gunakan skrip composer `serve`/`dev`.
upload_max_filesize = 25M
post_max_size = 512M
max_file_uploads = 100
max_input_vars = 5000
