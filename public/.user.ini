; Batas unggahan untuk lingkungan yang membaca .user.ini (mis. PHP-FPM/CGI).
; Untuk `php artisan serve`, gunakan skrip composer `dev`/`serve` atau set php.ini global.
upload_max_filesize = 25M
post_max_size = 256M
